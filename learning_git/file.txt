 writing all the git functionalities here.


git config --global user.name "" ---> for configuring user name
git config --global user.email "" ---> for configuration of account
git config --list ----> for knowing the credentials/configurations

git init ---> for initialization of repository
git add . ----> uploading the file into stagging area
mkdir filename ----> making new directory in our repo
notepad filename.txt ---> for creating new text file.

git status---> to know status of our repository
git commit -m "this is my initial commit" ---> for commiting the files that are in the stagging area.

git and GitHub are not same, git is the version control system and GitHub is online cloud where developers can commute and upload and access the file.

cd directoryname ---> changing the directory
mkdir directoryname --> making new directory

rm filename --> removes the file from directory.



till here commiting in local system is done, now we need to push our files to GitHub through git in terminal
1.we need to open new repo in our GitHub portal
2.git commit -m ""
3.git branch -M main ---> creating main branch
4.git remote add origin https://github.com/golinagababu700-gif/reponame.git ----> connecting to our GitHub portal
5.git push -u origin main


git clone url ---> cloning the repo

git branch -m newbranchname ---> creating new branch
notepad filename.txt ---> new file in new branch
git add .
git commit -m " this is new file in new branch upfdate"
git push -u origin newnranch

git restore . --->mistake happen go to previous.
git branch -d branchname ---> for deleting branches



